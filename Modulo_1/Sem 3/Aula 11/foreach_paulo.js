// const numeros = [10, 20, 30];

// for (const numero of numeros) {
//     conmsole.log({ numero });
// }

// numeros.forEach();

// // ----------------------------------------------------

// const somaArrow = (a, b) => (a, b);

// // VALOR, INDICE, LISTA
// function minhaFuncao(a, b, c) {
//     console.log(a, b, c);
// }

// minhaFuncao(1, 2, 3);

// const numeros = [10, 20, 30];

// numeros.forEach(minhaFuncao);

// numeros.forEach((valor, indice, lista) =>
//     console.log({ valor, indice, lista }),
// );

// // É IGUAL A :

// numeros.forEach(console.log);

// // ---------------------------------

// const numeros = [undefined, null, 'Paulo', '', 'Salvatore'];
// for (const nome of nomes) {
//     if (nome !== undefined && nome !== null && nome != '') {
//         nomesLimpos.push(nome);
//     }
// }

// console.log(nomesLimpos);

// const nomesLimpos2 = nomes.filter(Boolean);

// console.log({ nomesLimpos2 });
