// ARROW FUNCTION
// MESMA COISA QUE UMA FUNCAO
// Entrou recentemente no js, jeito mais moderno de usar uma função

const somar = (a, b) => {
    // SO DECLARAR A e B se tiver parametros, se nao ()
    const soma = a + b;
    return soma;
};

console.log(somar(15, 20));
